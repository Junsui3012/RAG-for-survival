// vite.config.js — Vite configuration with React plugin and API proxy to backend
// Step 1: Scaffold only — proxy setup for development
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  server: {
    proxy: {
      '/api': {
        target: 'http://localhost:5000',
        changeOrigin: true,
        secure: false,
      },
    },
  },
})