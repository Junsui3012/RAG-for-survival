// RegisterForm.jsx — Controlled form component for user registration
// Step 6: name, email, password, confirmPassword + strength indicator
export default function RegisterForm() {
  return <form>Register placeholder</form> // Step 6: full form UI + handlers added here
}