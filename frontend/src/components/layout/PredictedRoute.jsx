// ProtectedRoute.jsx — HOC component that checks JWT before rendering children
// Step 6: Redirects to /login if no JWT token
export default function ProtectedRoute() {
  return null // Step 6: full protection logic added here
}